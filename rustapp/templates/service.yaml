apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustapp.fullname" . }}
  labels:
    {{- include "rustapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rustapp.selectorLabels" . | nindent 4 }}
